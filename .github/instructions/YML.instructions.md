---
description: 'Agent directives for YAML formatting and linting.'
applyTo: '*.yml'
---

# Agent Directives — YAML Formatting & Linting

This file exists solely because `applyTo` can reference only one extension. For `.yml` files, follow [YAML.instructions.md](./YAML.instructions.md); every rule there applies here unchanged.
