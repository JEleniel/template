# Agent memory (redundant)

This file is a lightweight, repo-local backup of key conventions and navigation notes.

## Conventions

- JSON formatting is governed by `.prettierrc.json` (tabs, `tabWidth: 4`, `trailingComma: none`).

## Layout notes

- `Node/` contains the TypeScript configuration (`Node/tsconfig.json`) for NodeNext / ESNext.
- `Frameworks/SvelteKit/` contains template configuration files for SvelteKit projects.
- Other language templates live under `Go/`, `Java/`, `Python/`, and `Rust/`.
